# OpenStatus Scaffold

The repo includes an `ops/openstatus/` companion scaffold, not a vendored runtime.

Use it to monitor:

- `/api/health`
- `/`
- `/dashboard`

OpenStatus should consume the same health payload that powers the in-app dashboard.
