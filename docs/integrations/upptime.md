# Upptime Scaffold

The repo includes an `ops/upptime/` companion scaffold, not a vendored runtime.

Use it to monitor:

- `/api/health`
- `/`
- `/dashboard`

The starter keeps `/api/health` as the canonical machine-readable readiness target.
