# OpenStatus Companion Scaffold

This directory contains an OpenStatus-oriented scaffold for the same starter health surfaces.

- Update `.env.example` with your deployment values.
- Import `monitors.json` into your OpenStatus project.
- Keep `/api/health` as the canonical readiness target.
