# Upptime Companion Scaffold

This directory contains a starter-friendly Upptime reference configuration.

- Point `url` at your deployed application.
- Keep `/api/health` as the primary readiness target.
- Add `/` and `/dashboard` as public smoke targets.

Template files:

- `.upptimerc.yml`
